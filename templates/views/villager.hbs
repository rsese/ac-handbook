{{>head}}
<body>
  {{>header}}
  <main>
    <h1><a href="/">home</a> > <a href="/villagers">villagers</a> > {{name}}</h1>
    <img class="frame" src="{{image_uri}}" alt="{{image_alt}}" />
    <h2>Catch phrase 💭</h2>
    <blockquote>
      {{catch_phrase}}
    </blockquote>
  </main>
{{>footer}}
</body>
</html>