{{>head}}
<body>
  {{>header}}
  <main>
    <img class="frame" src="{{image_uri}}" alt="{{image_alt}}" />
    <h2>Catch phrase 💭</h2>
    <blockquote>
      {{catch_phrase}}
    </blockquote>
  </main>
{{>footer}}
</body>
</html>